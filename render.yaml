services:
  - type: web
    runtime: static
    name: animated-react
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20
